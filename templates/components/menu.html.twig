{% block menu %}
    <!-- LEFT SIDEBAR -->
    <div id="sidebar-nav" class="sidebar">
        <div class="sidebar-scroll">
            <nav>
                <ul class="nav">
                    <li>
                        <a href="{{ path("index") }}"
                                {% if app.request.get('_route') == 'index' %} class="active" {% endif %}>
                            <i class="lnr lnr-home"></i> <span>Dashboard</span></a>
                    </li>
                    <li>
                        <a href="{{ path("statistics") }}"
                                {% if app.request.get('_route') == 'statistics' %} class="active" {% endif %}>
                            <i class="lnr lnr-home"></i> <span>Statistics</span></a>
                    </li>

                    <li>
                        <a href="#subSettings" data-toggle="collapse" class="active" aria-expanded="true"><i
                                    class="lnr lnr-file-empty"></i> <span>Settings</span> <i
                                    class="icon-submenu lnr lnr-chevron-left"></i></a>
                        <div id="subSettings" class="collapse in" aria-expanded="true" style="">
                            <ul class="nav">
                                <li><a href="{{ path("settings-basic") }}" class="">Basic</a></li>
                                <li><a href="{{ path("settings-aws") }}" class="">AWS</a></li>
                                <li><a href="{{ path("settings-email") }}" class="">Email</a></li>
                            </ul>
                        </div>
                    </li>

                </ul>
            </nav>
        </div>
    </div>
    <!-- END LEFT SIDEBAR -->
{% endblock %}

